# 🔔 คู่มือระบบแจ้งเตือน "ตามด่วน"

ระบบช่วยเตือนเซลล์ว่า **วันนี้ควรโทรเคสไหนก่อน** + **ดีลไหนค้างต้องดัน** — ส่งเข้า LINE อัตโนมัติ

---

## ภาพรวม

- เป็น **ข้อความธรรมดา** (ไม่ใช่การ์ด Flex แบบเดิม)
- ส่ง **อัตโนมัติวันละ 2 รอบ: 9 โมงเช้า + บ่ายโมง**
- เรียงตามความด่วน (สมองตัวเดียวกับกล่อง "ตามด่วน" ในหน้าเซลล์)
- ส่งให้เซลล์แต่ละคน เฉพาะเคสของตัวเอง

---

## เนื้อหาที่ส่ง

แต่ละข้อความมี 2 ส่วน

**ลีดต้องตาม** (เลข lead + เหตุผล)
- NLD6-006 — ยังไม่โทรเลย
- TLD-9376 — ฮอท ค้าง 3 วัน
- BLD-5232 — ไม่รับสาย ลองครั้งที่ 3

**ดีลค้าง** (ชื่อลูกค้า + ทะเบียน + ค้างกี่วัน)
- สมชาย · ทะเบียน 3ขฉ3904 — รอผลไฟแนนซ์นาน 6 วัน
- มาลี · ทะเบียน 1กก234 — ค้างเซ็น 4 วัน

ท้ายข้อความมีลิงก์เข้าหน้าตัวเองของเซลล์

---

## สมองคิดลำดับยังไง

เคสจะ "ด่วน" มากขึ้นเมื่อ
- **ยังไม่โทรเลย** → ด่วนสุด (ลีดใหม่ต้องรีบโทร)
- **ฮอท × ค้างนาน** → ลีดคุณภาพดีที่ดองไว้นาน ลอยขึ้นบน
- **สถานะลูกค้า** สนใจมาก/ลังเล ขึ้นก่อน
- **ถึงรอบตาม (cadence)** → เพิ่งโทรไปจะ "พักไว้" ไม่เด้งซ้ำ
  - สนใจมาก 1 วัน · ลังเล 2 วัน · รอเงิน 3 วัน · ไม่รับสาย 1 วัน
- **ไม่รับสายเกิน 5 ครั้ง** → พักไว้ ไม่สแปมลูกค้า

ตัดออก (ไม่เตือน): เคสปิดแล้ว (คืนเคส/ยกเลิก/จ่ายใหม่) · จอง/ส่งมอบ · เคสเสีย

กรองเฉพาะ **เคสใหม่** = เดือนนี้ + 1 สัปดาห์ท้ายเดือนก่อน (ตัดของเก่า/ดีลตายทิ้ง)

---

## โหมดทดสอบ vs ส่งจริง

ตอนนี้อยู่ **โหมดทดสอบ** — ส่งเข้าไลน์แอดมินก่อน (ยังไม่ส่งเซลล์จริง)

| โหมด | ส่งไปไหน |
|---|---|
| ทดสอบ (ตอนนี้) | ไลน์แอดมินคนเดียว |
| ส่งจริง | เซลล์แต่ละคนเข้าไลน์ตัวเอง |

**สลับเป็นส่งจริง:** บอกผู้ดูแลระบบให้แก้ `_FU_TEST_TARGET` เป็น `""` ใน [views.py](dashboard/views.py) (ในฟังก์ชัน `cron_tick`) แล้ว deploy

---

## ส่งแมนนวล (ส่งเดี๋ยวนี้ ไม่รอ 9 โมง)

แอดมินกดส่งเองได้

1. หน้า dashboard → เมนูจัดการ → **"แจ้งเตือนตามด่วน"**
2. แท็บ **"ส่งทันที (One-time)"**
3. ติ๊กเลือกเซลล์ที่จะส่ง (หรือ "เลือกทั้งหมด")
4. อยากทดสอบก่อน → ติ๊ก **Test mode** + ใส่ user_id ตัวเอง (จะส่งเข้าไลน์เราแทน ไม่ถึงเซลล์)
5. กด **ส่ง**

ตารางจะโชว์ โทรแล้ว / ยังไม่โทร / ต้องตาม ของแต่ละเซลล์ ช่วยตัดสินใจว่าจะส่งใคร

> หมายเหตุ: เซลล์ที่ไม่มีเคสใหม่ จะไม่มีข้อความส่ง (ไม่มีอะไรต้องเตือน)

---

## ปรับแต่งได้ (แก้ในโค้ด)

ทั้งหมดอยู่ใน [views.py](dashboard/views.py) `cron_tick` และ [fetch_dashboard.py](dashboard/services/fetch_dashboard.py) `build_followup_messages`

| อยากเปลี่ยน | แก้ที่ |
|---|---|
| เวลาส่ง (9/13) | `_FU_TIMES` ใน cron_tick |
| ส่งจริง/ทดสอบ | `_FU_TEST_TARGET` ใน cron_tick |
| จำนวนเคสต่อข้อความ | `build_followup_messages(max_leads=5, max_deals=5)` |
| จังหวะตาม (cadence) | `_FU_CADENCE` |
| ไม่รับสายพักที่กี่ครั้ง | `_FU_NOANS_CAP` (5) |
| โดเมนลิงก์ | `_FU_BASE` |

---

## ที่ต้องมีให้พร้อม

- `LINE_CHANNEL_ACCESS_TOKEN` ตั้งใน env (Vercel) — ไม่งั้นส่งไม่ได้
- เซลล์ต้องมี **LINE user_id** ในชีตพนักงาน (คนที่ไม่มีจะถูกข้าม)
- cron-job.org ยิง `/api/cron/tick` ทุก 1 นาที (อันเดิมที่ใช้อยู่ — ตัวเดียวกับ sync ข้อมูล)

---

## ถ้าไม่ส่ง / มีปัญหา

- **ไม่มีข้อความเลย** → เช็คว่า cron ยิง /api/cron/tick อยู่ไหม + token ตั้งไหม
- **เซลล์บางคนไม่ได้รับ** → เช็คว่ามี LINE user_id ในชีต (ตารางในโมดอลโชว์ ✓/✗)
- **อยากดูว่าจะส่งอะไร** → ใช้ "ส่งทันที" + Test mode ส่งเข้าไลน์ตัวเองดูก่อน
- **ข้อมูลเก่า/เคสที่ลบยังขึ้น** → ข้อมูล sync ทุก ~1 นาที รอสักครู่

---

_อัปเดตเมื่อแก้ระบบแจ้งเตือน · ดูเพิ่ม: [SYSTEM-DESIGN.md](SYSTEM-DESIGN.md) · [CLAUDE.md](CLAUDE.md)_
