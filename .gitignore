# Secrets — .env ต้องไม่ขึ้น git (ถอดออกจาก tracking แล้ว มิ.ย.69)
# secrets เก็บใน local .env + Vercel env vars เท่านั้น
.env
.env.local
.env.*.local
*.pem
*.key
# helper สำหรับดู env values — local only (inline comment ใช้ไม่ได้ ต้องแยกบรรทัด)
show_env.py

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Data exports — มีข้อมูลส่วนบุคคล (เบอร์/ชื่อลูกค้า) ห้ามขึ้น git (PDPA)
exports/
*.export.json
test_render.html

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
