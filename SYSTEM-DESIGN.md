# 🏗️ ระบบ Oxlet — ภาพรวมการทำงานทั้งหมด

> **หลักคิด:** เว็บไม่ใช่ "จอดูข้อมูล" — เว็บคือ **ประตูควบคุม** ที่คนกรอก/อัพเดท/สั่งการได้
> ข้อมูลไหล **ทางเดียว** เสมอ (ไม่มี 2-way sync ที่ชนกัน) → ไม่งง ไม่พัง ไม่หาย

---

## 1. ภาพใหญ่ — ใครทำอะไร เชื่อมยังไง

```
        เซลล์                แอดมิน/ผู้บริหาร            พนักงานหลังบ้าน
          │                       │                          │
   กรอก/อัพเดทเคส            ดู + สั่งการ + ตั้งค่า          (ช่วงเปลี่ยนผ่าน:
   กรอกฟอร์มไฟแนนซ์                                          ยังกรอกใน Sheet)
          │                       │                          │
          ▼                       ▼                          ▼
  ╔═══════════════════════════════════════════════╗   ┌─────────────────┐
  ║          เว็บแอป  (Django @ Vercel)            ║   │  Google Sheets  │
  ║                                                ║   │   (5 ไฟล์)      │
  ║   ┌──────────────┐        ┌────────────────┐   ║   │  พนักงานกรอกตรง  │
  ║   │ ฟอร์มกรอก     │        │   Dashboard     │   ║   └────────┬────────┘
  ║   │ ✍️ เขียน/คุม   │        │   👁️ อ่าน/ดู     │   ║            │ อ่าน (mirror)
  ║   └──────┬───────┘        └────────▲───────┘   ║            ▼
  ╚══════════╪═════════════════════════╪═══════════╝   ┌─────────────────┐
             │ ✍️ เขียน                 │ อ่านเร็ว        │    Supabase      │
             │                         └───────────────│  (DB + mirror)  │
             ▼                                         └─────────────────┘
   ┌──────────────────────────────┐
   │ ปลายทางที่เว็บ "สั่งการ" ได้:    │
   │  • Supabase  (เก็บฟอร์ม/ถาวร)   │
   │  • Google Sheets (เขียนกลับ)    │
   │  • n8n → ชีตนิ่ง / automation    │
   │  • LINE  (push แจ้งเตือน Flex)  │
   └──────────────────────────────┘
```

**3 ชั้นของระบบ:**
1. **คนใช้** — เซลล์ / แอดมิน / พนักงาน
2. **เว็บแอป** — มี 2 ฝั่ง: ✍️ ฟอร์มกรอก (ควบคุม) + 👁️ Dashboard (ดู)
3. **ที่เก็บ/ปลายทาง** — Supabase (ฐานถาวร) · Google Sheets (ชีต) · n8n · LINE

---

## 2. สองเส้นทางข้อมูล

### 👁️ เส้น READ — ดู Dashboard (เร็ว)
```
Google Sheets ──(sync ทุก 2-5 นาที)──> Supabase mirror ──> Dashboard
   (พนักงานกรอก)                          (สำเนาอ่านเร็ว)        (เซลล์/แอดมินดู)
```
- Sheets = ความจริง · Supabase = สำเนาไว้โหลดเร็ว · พังก็ rebuild ใหม่ได้ ไม่มีข้อมูลหาย

### ✍️ เส้น WRITE — ควบคุม/สั่งการ (★ ที่ทำให้ "ไม่ใช่แค่ดู")
```
คนกรอกในเว็บ
   │
   ① เขียน Supabase ก่อน  ✔ commit (ข้อมูลปลอดภัยแล้วตรงนี้)
   │
   ② ค่อยเขียนต่อ (best-effort, ล้มแล้ว retry ได้):
        • Google Sheets  (Django เขียนตรง — ทน tab/คอลัมน์เปลี่ยน)
        • n8n → ชีตนิ่ง / แจ้งเตือน
   │
   ③ ตอบกลับ client "บันทึกแล้ว ✓"
```
> **กฎเหล็ก:** เก็บ DB ก่อนเสมอ → ต่อให้ชีต/n8n ล้มทีหลัง **ข้อมูลไม่หาย**

---

## 3. ฟีเจอร์ "ควบคุม" — พิสูจน์ว่าไม่ใช่แค่ดู

| ฟีเจอร์ | ใครใช้ | เขียนไปไหน | สถานะ |
|---|---|---|---|
| อัพเดทเคส (คอลัม S "มากรอกชีตกันเถอะ") | เซลล์ | → Google Sheet จริง | ✅ ทำแล้ว |
| ฟอร์มเช็คไฟแนนซ์ก่อนเซ็น | เซลล์ | → Supabase + LINE Flex | ✅ ทำแล้ว |
| ฟอร์มยื่นสินเชื่อ | เซลล์ | → Supabase + LINE Flex | ✅ ทำแล้ว |
| สแกนเอกสาร (Gemini OCR) เติมฟอร์ม | เซลล์ | → เติมฟอร์มอัตโนมัติ | ✅ ทำแล้ว |
| ตั้งเป้า/ทีมเซลล์ | แอดมิน | → Google Sheet "ตั้งค่าเซลล์" | ✅ ทำแล้ว |
| ตั้งเวลาส่ง LINE อัตโนมัติ | แอดมิน | → Google Sheet "ตั้งเวลาส่ง" | ✅ ทำแล้ว |
| ย้าย/เปลี่ยนไฟล์ชีต (ขึ้นปีใหม่) | แอดมิน | → Supabase config | ✅ ทำแล้ว |
| **ฟอร์มรับ lead ใหม่** | เซลล์/แอดมิน | → Supabase + ชีต | 🔜 ถัดไป |
| **ฟอร์มตีสถานะลูกค้า (คอลัม Z)** | เซลล์ | → Google Sheet | 🔜 ถัดไป |
| **ฟอร์มบันทึกจอง/ปล่อย** | เซลล์ | → Supabase + ชีต | 🔜 ถัดไป |

→ ตอนนี้ระบบ **เขียน/ควบคุมได้ 7 อย่างแล้ว** ไม่ใช่แค่ดู

---

## 4. ปัญหา "คอลัมน์เปลี่ยนเรื่อยๆ" — จัดการยังไง

```
คนย้าย/แก้คอลัมน์ในชีต
        │
        ▼
ระบบหาคอลัมน์จาก "ชื่อหัวตาราง" (ไม่ใช่ตำแหน่ง)
        │
   ┌────┴─────────────────────────┐
   ▼                              ▼
เจอ → ทำงานปกติ ✅          ไม่เจอ (เปลี่ยนชื่อหัว)
                                  │
                                  ▼
                        ⚠️ เตือนแอดมิน "หาคอลัมน์ X ไม่เจอ"
                        → แก้ alias 1 บรรทัด (ไม่ต้องรื้อ)
```
- **ย้ายตำแหน่งคอลัมน์** = ระบบปรับเอง ไม่ต้องทำอะไร
- **เปลี่ยนชื่อหัว** = ระบบเตือน + แก้ 1 บรรทัด
- **ปีหน้าย้ายเข้า DB** = ปัญหาหายถาวร (กรอกผ่านฟอร์ม ไม่มีคอลัมน์ให้ขยับ)

---

## 5. แผนเดินทาง 3 เฟส

```
เฟส A — ตอนนี้ (Sheets เป็นหลัก + เว็บเขียนกลับได้)
  ████████████░░░░░░░░  พนักงานกรอกชีต · เว็บ mirror + เขียนกลับบางส่วน
        │  ค่อยๆ เพิ่มฟอร์มกรอกในเว็บทีละชนิด
        ▼
เฟส B — ระหว่างปี (ย้ายการกรอกมาเว็บทีละอย่าง)
  ████████████████░░░░  ข้อมูลใหม่กรอกในเว็บ → DB + ชีต · ชีตเริ่มเป็นสำเนา
        │  พอครบทุกชนิด
        ▼
เฟส C — ~1 ปี (ฐานข้อมูลถาวร เป็นหลัก)
  ████████████████████  DB = ความจริง · ฟอร์มล็อกโครงสร้าง · ชีต = export
```

**10 binding ปัจจุบัน → ~10 ตารางใน DB ถาวร** (สคีมาเริ่มจากตรงนี้ได้เลย)

---

## 6. บทบาทผู้ใช้

| บทบาท | ดูอะไร | ทำอะไรได้ |
|---|---|---|
| **เซลล์** | เคสตัวเอง (หน้า `/s/<token>/`) | อัพเดทเคส · กรอกไฟแนนซ์/สินเชื่อ · สแกนเอกสาร |
| **ผู้บริหาร** | ทุกอย่าง + Analytics | ดูภาพรวม · impersonate ดูมุมเซลล์ |
| **แอดมิน** | ทุกอย่าง + เครื่องมือ | ตั้งเป้า/ทีม · ส่ง LINE · ย้ายแหล่งข้อมูล · ตั้งเวลา |

---

## 7. ความปลอดภัย

- Service account (`n8n-sheets@...`) อ่าน+เขียนชีต — key อยู่ server เท่านั้น
- Supabase secret key อยู่ server เท่านั้น (ไม่โผล่ frontend)
- ฟอร์มทุกตัวตรวจ token เซลล์ + ownership (แก้ได้แค่เคสตัวเอง)
- n8n webhook ใส่ secret กันยิงปลอม · ทุกอย่างเป็น HTTPS

---
_เอกสารนี้คือ "พิมพ์เขียว" ของระบบ — อัปเดตเมื่อสถาปัตยกรรมเปลี่ยน_
